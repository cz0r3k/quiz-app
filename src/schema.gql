# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

interface Question {
  id: Int!
  task: String!
}

type Quiz {
  id: Int!
  name: String!
  questions: [Question!]!
}

type Sorting implements Question {
  id: Int!
  task: String!
  order: [String!]!
}

type MultipleCorrectAnswers implements Question {
  id: Int!
  task: String!
  answers: String!
  correctAnswers: [String!]!
}

type PlainTextAnswer implements Question {
  id: Int!
  task: String!
  correctAnswer: String!
}

type SingleCorrectAnswer implements Question {
  id: Int!
  task: String!
  answers: [String!]!
  correctAnswer: String!
}

type Query {
  quizzes: [Quiz!]!
  quiz(id: Int!): Quiz!
  question(quizId: Int!, questionId: Int!): Question!
}

type Mutation {
  addQuiz(input: QuizInput!): Quiz!
}

input QuizInput {
  name: String!
  questions: [QuestionInput!]
}

input QuestionInput {
  task: String!
}